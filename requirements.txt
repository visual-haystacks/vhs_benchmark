# Important libraries with versions
lmdeploy~=0.6
torch
torchvision
transformers~=4.45
accelerate
# flash-attn~=2.6
outlines==0.0.46 # Specifiy cuz this error: https://github.com/dottxt-ai/outlines/issues/1198

tqdm
nltk
pandas
hydra-core
scipy               # used by the OWL-v2 detector
qwen-vl-utils       # used by the qwen2-vl model
icecream            # used by the mPLUG OWL model
google-generativeai # used by gemini 
anthropic           # used by claude
openai              # used by gpt